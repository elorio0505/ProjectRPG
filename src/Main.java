import RPGGame.Controller.GameController;

public class Main {
    public static void main(String[] args) {
        GameController game = new GameController();    // Create an instance of GameController
        game.startGame();             // Start the game by calling the startGame method on the GameController instance
    }
}

package RPGGame.Event.Abstracts;

import RPGGame.Controller.PrimaryScene;
import RPGGame.Player;

public class GeneratedEvent extends Event {

    @Override
    public boolean run(Player player, PrimaryScene scene) {
        return false;
    }
}
